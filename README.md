Questions available here https://adventofcode.com/

To run:
- Navigate to `advent-of-code` directory
- Run commands `cargo build` and `cargo run` and view answer from output
- To get a different question's answer, update the question input in `main()` and run the cargo commands
